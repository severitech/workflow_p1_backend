# ── Local ──────────────────────────────────────────────
SERVER_PORT=8080
MONGO_DB=workflow_db

# ── Azure Cosmos DB ────────────────────────────────────
MONGO_PASSWORD=75399036Dps

# ── Google Cloud (Cloud Run + Cloud Atlas / Flex) ──────
# MONGO_URI=mongodb+srv://<user>:<pass>@cluster.mongodb.net/workflow_db
# GCP_PROJECT_ID=mi-proyecto-gcp
# GCP_REGION=us-central1
# SERVICE_NAME=workflow-backend
# IMAGE=gcr.io/${GCP_PROJECT_ID}/${SERVICE_NAME}

# ── JWT ───────────────────────────────────────────────
JWT_SECRET=cambia_este_secreto_en_produccion
JWT_EXPIRATION_MS=86400000

# ── CORS ─────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:4200,https://tu-frontend.web.app
